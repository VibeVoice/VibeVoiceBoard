*.iml
.idea
.gradle
.kotlin
local.properties
.DS_Store
Gemfile
build
app/build
app/release
app/.cxx
app/.attach_*
fastlane/Appfile
tools/*.txt
*.patch
*.diff
*.sh
!tools/build-and-deploy.sh
!tools/hooks/*.sh
tools/make-emoji-keys/bin/
tools/make-emoji-keys/build/

# Local secrets
.env
.env.*
tools/last-build.log

# Local build SDK, crash logs, debug logs
android-sdk/
crash_reports/
*.zip
*.log
tools/last-build.log
tools/build-*.log
tools/.build.lock

# Release signing material - never commit
keystore.properties
*.jks

# The Play service-account key. It can publish to the store as us, so it is the one file in this
# tree whose leak would be worse than the signing key's: that one only signs, this one ships.
play-service-account.json
*.keystore
.wt/
