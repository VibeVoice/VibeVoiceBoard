<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the staged copy is shared, never the live log: the writer thread appends to that one
         while a mail app would be reading it. -->
    <cache-path
        name="exported_logs"
        path="logs/"/>
</paths>
